<!DOCTYPE html>
<html>
<head>
  <style>
    .container {
      width: 800px;
      font-family: Arial, Helvetica, sans-serif;
    }

    span {
      margin: 10px 0;
      display: block;
    }

    .movie-title {
      font-size: 24px;
      font-weight: bold;
      color: #333;
      margin-bottom: 20px;
    }

    .movie-info {
      margin: 15px 0;
      color: #555;
    }

    .movie-image {
      margin-top: 20px;
    }

    img {
      max-width: 300px;
      border-radius: 5px;
    }

    .footer {
      margin-top: 30px;
      font-size: 14px;
      color: #999;
    }
  </style>
</head>
<body>
  <div class="container">
    <span>Olá, {{name}}!</span>

    <span>O filme que você cadastrou está estreando hoje!</span>

    <div class="movie-title">{{movieTitle}}</div>

    <div class="movie-info">
      <p><strong>Título Original:</strong> {{movieOriginalTitle}}</p>
      <p><strong>Data de Lançamento:</strong> {{movieReleaseDate}}</p>
      <p><strong>Descrição:</strong> {{movieOverview}}</p>

      {{#if movieTrailerUrl}}
      <p><strong>Trailer:</strong> <a href="{{movieTrailerUrl}}">Assista ao trailer</a></p>
      {{/if}}
    </div>

    {{#if movieImageUrl}}
    <div class="movie-image">
      <img src="{{movieImageUrl}}" alt="{{movieTitle}}">
    </div>
    {{/if}}

    <span class="footer">
      Não perca! Este filme que você adicionou à sua coleção está estreando hoje.
    </span>
  </div>
</body>
</html>
